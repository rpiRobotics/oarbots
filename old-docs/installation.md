# Installation Instructions

## Downloading External Packages

1. Clone the repository into the ROS2 workspace folder
2. To install external packages that are *not* available through package managers, `cd` into `./src` and run `vcs import < ../packages.repos .`