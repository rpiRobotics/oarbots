[Unit]
Description=Headless X server for Kinect GLX/depth engine (NVIDIA GPU)

[Service]
Type=simple
ExecStart=/usr/bin/Xorg :2 -config /etc/X11/xorg-headless.conf -noreset -ac -nolisten tcp
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target