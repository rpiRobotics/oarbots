jobs:
  build_and_test_ros2:
    runs-on: ubuntu-24.04
    steps:
      - uses: ros-tooling/setup-ros@v0.7
        with:
          required-ros-distributions: jazzy
      - uses:
          package-name: oarbots
          target-ros2-distro: jazzy
          vcs-repo-file-url: packages.repos